# このプロジェクトについて

消化器内科医（本人）が、消化器内科分野の医学論文を効率よくキャッチアップするためのアプリ。
PubMed / Europe PMC など権威ある情報源から**インパクトファクターの高い誌・被引用数の多い論文**を優先して自動収集し、各論文は「タイトル」ではなく「結論」を大きく見せるサムネイル一覧で表示、タップすると全文を日本語訳して読める、という体験を目指す。毎晩ローカルPCが自動で収集〜要約・翻訳〜公開まで行い、iPhoneからいつでも最新の状態を見られるようにする。

（★当初は「専門外の論文をキャッチアップしたい」という依頼だったが、本人に確認の上「消化器内科の論文のみ」に方針転換・確定。詳細は下記「確定事項」参照。）

`AIの作業場`（Xポスト・note発信ビジネス用のリポジトリ）とは別の独立プロジェクトとして、Desktop直下にこのフォルダを新規作成した。

---

## 実現可能性まとめ（重要な制約）

- **PubMed（NCBI E-utilities）**：無料の公式APIで自動取得可能。ただし取得できるのは基本的に「タイトル・著者・雑誌名・アブストラクト（抄録）」まで。全文ではない。
- **NEJMの全文**：公式の一般公開APIは存在しない。全文は購読者ログインが必要な有料コンテンツで、スクレイピングは利用規約違反かつBot対策でほぼ確実にブロックされる。→ **全文の自動取得は現実的に不可能**。
- **オープンアクセス論文の全文**：PMC（PubMed Central）にオープンアクセスで全文が置かれている論文は、正規のAPI経由で全文取得・自動翻訳が可能。
- **現実的なMVPの落とし所**：
  - 自動収集・自動要約・自動翻訳の対象は「タイトル＋アブストラクト」。ここは完全自動化できる。
  - 「全文を読みたい」場合は、(a) オープンアクセス論文ならアプリ内で全文翻訳、(b) 購読が必要な論文（NEJM等）は本文へのリンクを表示し、本人が手動でPDFを入手→アプリにアップロードすれば翻訳、という2段構えにする（未実装）。

---

## 確定事項

- **アプリ形態：Web/PWA。** スマホのブラウザから開ける形にする。iPhoneのSafariで「ホーム画面に追加」すればアプリのように使える（実装済み、後述）。
- **収集対象：消化器内科の論文のみ。**（★当初「専門外の論文」という依頼だったが、本人に確認の上「消化器内科の論文を探してきてほしい」に変更・確定。専門外キャッチアップの構想は撤回。）
- **ホスティング：ローカルPC＋GitHub Pages。**（詳細は「ホスティング方式」の章を参照。当初「Artifactで再公開」という設計だったが、実際には無理だと判明したため2026-08-15にGitHub Pagesへ変更。）
- **NEJM等の個人購読：なし。** → 全文の自動翻訳はオープンアクセス論文のみ。購読が必要な論文はPubMedリンクの提示に留め、全文が必要な場合は本人が手動でPDFをアップロードする運用（未実装）。
- **LINE通知：当面スキップ。** まずはWebページを直接見る運用にする。必要になったら既存のX自動化と同じLINE公式アカウントを使うか、新規アカウントを作るか改めて相談する。

---

## ホスティング方式（確定・2026-08-15、設計修正あり）

**当初の設計ミス：** 「毎晩ヘッドレスClaude（`claude -p`）がArtifact機能でページを再公開する」という設計を立てていたが、既存のXポスト自動化スクリプト（`AIの作業場/.local/run-daily-post.ps1`）を実際に確認したところ、ヘッドレス実行では `--tools "Bash,Read,Write,Edit,Glob,Grep"` のようにツールが制限されており、**Artifactツールは使えない**ことが判明した。そもそも既存のX自動化もArtifactは使っておらず、ファイル書き込み＋git push＋curlという地に足の着いた方法で成立している。

**修正後の方式：GitHub Pagesで公開する。**

- リポジトリ：`taisuke1997t-code/gi-paper-digest`（**public**。内容は論文の要約・翻訳のみで個人情報を含まないため、本人の合意のもと公開リポジトリとした）
- 公開ページ：`docs/index.html` を配信する設定で GitHub Pages を有効化 → `https://taisuke1997t-code.github.io/gi-paper-digest/` で閲覧可能
- 毎晩の自動実行フロー：ヘッドレスClaudeが `docs/articles.json` に今日選定した論文を追記し、`資料/使用履歴.md` を更新し、`git commit && git push` する → 数分でGitHub Pagesに反映される（**`docs/index.html`自体は日々変更しない固定テンプレート**。詳細は「データ構造とアーカイブ」参照）
- この方式なら、既存のX自動化と全く同じ「ファイル操作＋git push」パターンで完結し、Artifactツールの可用性に依存しない。

---

## iPhoneでアプリのように使う（実装済み・2026-08-15）

`docs/index.html` に以下を実装済み：

- ホーム画面追加用のアイコン（apple-touch-icon、teal背景に「胃」の文字、SVGをdata URIで埋め込み）
- `apple-mobile-web-app-capable` 等のメタタグにより、ホーム画面から起動するとブラウザのアドレスバー等が非表示のフルスクリーン表示になる
- ノッチ・ステータスバー対応の safe-area 余白
- 初回訪問時（iOS Safariかつホーム画面未追加の場合のみ）に「ホーム画面に追加すると、アプリのように使えます」という案内バナーを表示（一度閉じれば`localStorage`に記録され再表示しない）

**追加手順（自動インストールはiOSの仕様上できないため手動）：** iPhoneのSafariでページを開く → 共有ボタン → 「ホーム画面に追加」

---

## 論文の選定基準（確定・2026-08-15、選定方法を修正）

**単純な新着順ではなく、インパクトファクター（IF）が高い誌・被引用数の多い論文を優先する。** 「最新の注目論文」と「クラシックな名論文」の2種類を組み合わせて表示する。

### 対象誌のIFティア（参考値・要検証、年1回程度の見直しが必要）

- **Tier S**（総合誌）：New England Journal of Medicine（IF≈96）, The Lancet（IF≈98）, JAMA（IF≈63）
- **Tier A**（GI専門誌トップ）：The Lancet Gastroenterology & Hepatology（IF≈35）, Gastroenterology（IF≈29）, Journal of Hepatology（IF≈26）, Gut（IF≈23）, Nature Reviews Gastroenterology & Hepatology（IF≈45、総説誌）
- **Tier B**：Hepatology（IF≈12-14）, Clinical Gastroenterology and Hepatology（IF≈11）, The American Journal of Gastroenterology（IF≈10）, Gastrointestinal Endoscopy（IF≈9）, Endoscopy（IF≈10）

### 「最新の注目論文」の探し方

- **PubMed E-utilities**（`esearch` → `efetch`、APIキー不要・無料）を使用。
- 対象誌をTier S・Aに絞り、直近30日程度でフィルタ。該当がなければTier Bにフォールバック。
- `Publication Type` が「Randomized Controlled Trial」「Meta-Analysis」「Practice Guideline」「Review」などを優先し、「Letter」「Comment」「Case Report」は除外する（アブストラクトが無いことが多く、影響度も低い傾向のため）。

### 「クラシックな名論文」の探し方（★2026-08-15に方式変更）

当初は静的にキュレーションした「クラシック論文ライブラリ」ファイルを作る想定だったが、本人から**「クラシック論文も毎回探してきてほしい。同じ論文が短期間で被るのは困るので、同じ論文は1か月以内には出ないようにしてほしい」**という要望があり、**PubMedと同様に毎回動的に検索する方式**に変更した。

- **Europe PMC REST API**（`https://www.ebi.ac.uk/europepmc/webservices/rest/search`、無料・認証不要）を使用。このAPIは`sort=CITED desc`で**被引用数（citedByCount）順にソートできる**ため、「本当に影響力の大きい論文」を機械的に見つけられる。PubMed E-utilitiesにはこの機能がないため、クラシック論文の発見にはEurope PMCを使う。
- クエリ例（動作確認済み・2026-08-15）：
  ```
  query = (JOURNAL:"Gastroenterology" OR JOURNAL:"Gut" OR JOURNAL:"Hepatology" OR JOURNAL:"J Hepatol" OR JOURNAL:"Lancet Gastroenterol Hepatol" OR JOURNAL:"Clin Gastroenterol Hepatol" OR JOURNAL:"Am J Gastroenterol") AND PUB_YEAR:[2010 TO <今年-2>]
  sort = CITED desc
  ```
  Tier S誌（NEJM/Lancet/JAMA）の消化器内科領域の名論文も拾いたい場合は、`MESH_MAJOR`によるGI関連トピック絞り込みを追加するか、著者・トライアル名をヒントに個別検索する（2026-08-15時点でこの一般誌向けの自動クエリは未確定・要調整。今回のデモでは手動で4本を検索して見つけた）。
- **クラシック論文は2010年以降に限定する。** 当初1980〜1990年代の論文（H.ピロリ発見など）を投入していたが、本人から「さすがに古すぎる」とフィードバックがあり、2010年以降に変更した。
- 上位候補（被引用数順に十数〜数十件）を取得し、`資料/使用履歴.md`で1か月以内に使用済みのPMIDを除外した上で選ぶ。

### 重複防止ルール（確定・2026-08-15）

- **`docs/articles.json`が重複防止チェックの正（authoritative）データ。** 各エントリの`runDate`を見て、直近30日以内に使われたPMIDを候補から除外する。`資料/使用履歴.md`は人間が読むための簡易ログとして並行して維持するが、判定の根拠は`articles.json`側とする。
- **同じPMIDは、直近30日以内は再掲載しない。**（Xの自動化の「60日ルール」とは異なり、このプロジェクトでは30日とする——本人の指定。）
- 30日以内に未使用の候補が枯渇した場合は、最も使用日が古いものから使ってよい（フォールバック、Xの自動化と同じ考え方）。

### 表示比率

- **デフォルト比率：クラシック論文8割・最新の注目論文2割**（例：5本なら4本がクラシック、1本が最新）。この比率は自動収集スクリプト側の選定ロジックが適用する（クライアント側の調整UIは廃止——後述「データ構造とアーカイブ」参照）。

---

## データ構造とアーカイブ（確定・2026-08-15）

**背景：** 当初、`docs/index.html`に当日分の論文カードを直接埋め込み、毎日の自動更新でその`<ul>`部分を丸ごと書き換える設計だった。しかしこれだと**前日以前に載った論文が新しい実行のたびに消えてしまう**ことが、実際に自動化スクリプトを2回実行した際に発覚した（本人からのフィードバックで判明）。過去の論文もずっと見られるようにしたい、検索もしたい、という要望を受けて、**データと表示を分離する構成に変更した**。

### `docs/articles.json`（唯一のデータソース）

- これまでに公開した**全論文が蓄積される、追記専用（append-only）のJSON配列**。
- 1エントリ＝1論文。スキーマ：
  ```
  {
    "pmid": "42425127",
    "runSeq": 2,          // 実行回ごとに増分する連番。同じ実行で選ばれた論文は同じrunSeqを持つ
    "runDate": "2026-08-15", // その論文が選定された日付(YYYY-MM-DD)
    "type": "recent",     // "recent" または "classic"
    "journal": "Lancet Gastroenterol Hepatol",
    "ifTag": "IF≈35",     // recentのみ。classicは null
    "year": null,          // classicのみ発表年(数値)。recentは null
    "metaNote": "2026-09号", // recentは掲載日/号、classicはトライアル名等の短い説明
    "conclusion": "...",  // 結論見出し
    "stat": "...(HTML可、<b>で強調)",
    "titleJa": "...",     // タイトル和訳(「（原題和訳：」の接頭辞は含めない、表示時に付与)
    "bodyJa": "...(<b>見出し：</b>形式、段落は\n\nで区切る)",
    "impactNote": "...",  // classicのみ、影響力の解説。recentは null
    "titleOriginal": "英語原題",
    "pubmedUrl": "https://pubmed.ncbi.nlm.nih.gov/xxxxx/"
  }
  ```
- **自動収集スクリプトは、このファイルの末尾に今日選定した論文オブジェクトを追記するだけ。既存エントリは絶対に変更・削除しない。**
- 追記後は必ずJSON構文を検証してからcommit・pushすること（壊れたJSONを公開しないため）。

### `docs/index.html`（固定テンプレート、日次更新の対象外）

- ページ読み込み時に`fetch('articles.json')`でデータを取得し、JavaScriptで描画する。
- **「今日のダイジェスト」**：`articles.json`の中で最大の`runSeq`を持つエントリ群を表示。
- **「アーカイブ・検索」**：全エントリを新しい順（`runDate`で日付見出しを付けてグルーピング）に表示。テキスト検索ボックス（結論・タイトル・誌名・全文訳を対象に部分一致検索）と、タイプ絞り込み（すべて／クラシック／最新）のチップ、「もっと見る」ボタンによるページネーション（10件ずつ）を実装済み。
  - **検索の表記ゆれ吸収（確定・2026-08-15）：** 「大腸がん」で検索しても「大腸癌」がヒットするよう、検索クエリ・本文とも(1)カタカナ→ひらがな変換、(2)「がん」→「癌」への統一、を行ってから部分一致させている（`normalizeJa()`関数）。本人からのフィードバックで追加。
- **自動収集スクリプトはこのファイルを日々編集する必要がない。** データを追加するだけで表示は自動的に更新される。これにより、以前あった「HTMLの特定箇所だけを壊さず書き換える」というリスクの高い作業がなくなった。
- 旧バージョンにあった「表示設定」パネル（クラシック比率スライダー）は、この構成変更に伴い廃止し、検索・フィルタ機能に置き換えた。

---

## サムネイル・カードのデザイン仕様（確定・2026-08-15）

タイトルではなく**結論を主役にする**。理由：タイトルだけでは何が分かった論文か伝わらないため。

- **メインの見出し＝結論**：「〇〇には△△がおすすめ」「〇〇で■■のリスクが上昇」のような、読んで一目で要点が伝わる日本語の一文（大きく・太く表示）。
- 結論を補強する主要な数値（例：ハザード比、寛解率など）を結論の下に1〜2行で添える。
- **論文タイトル（原題の和訳）は結論より小さく、カッコ書きで一番下に表示**。あくまで参照用の添え書きという扱い。
- 誌名・IFタグ・掲載日・PMIDは結論の上に小さく表示し、「なぜこの論文が選ばれたか」を一目で分かるようにする。クラシック論文には`CLASSIC 2013`のような発表年タグを付ける。
- タップ（クリック）すると全文の日本語訳とPubMedへのリンクが開くアコーディオン形式。
- 実装：`docs/index.html`（表示テンプレート）＋`docs/articles.json`（データ）。公開URL：`https://taisuke1997t-code.github.io/gi-paper-digest/`

### レイアウト改修（確定・2026-08-15、本人フィードバックで3点修正）

1. **導入文が長すぎる** → masthead内の1行（最新更新日＋一言）に短縮。以前あった色付きの説明ボックス（`.note`）は廃止。
2. **カード同士の境目が薄い緑背景に埋もれて見づらい** → 各論文カードを白背景（`--surface`）＋枠線＋角丸12pxの独立したカードにし、カード間に余白を設けた（以前は罫線のみで区切る新聞リスト風レイアウトだったが、境界が視認しにくかった）。ダークモードのカード背景・罫線も少し明るめに調整してコントラストを確保。
3. **検索するのに下までスクロールするのが面倒** → 右上に固定表示（sticky）のハンバーガーメニュー（☰）を設置。タップすると右からスライドインするドロワーが開き、その中に検索ボックス・タイプ絞り込みチップ・**日付チップ**（実際にデータがある日付だけを新しい順にボタン表示、タップでその日の論文に絞り込み）・アーカイブ一覧をすべて集約。メインページは「今日のダイジェスト」だけになり、スクロールなしでいつでも検索にアクセスできる。

### モバイルでカードが画面右にはみ出すバグ（2026-08-15発生・修正済み）

本人からスマホのスクリーンショット付きで報告。原因は`.art-stat`・`.art-title`に設定していた`max-width:56ch`——**`ch`単位は日本語フォントだと文字幅の計算がずれて実際よりかなり幅広になることがあり**、カード全体が画面外にはみ出して右端が見切れていた。
修正：
- 該当箇所の`56ch`指定を撤去（外側の`.page`が`max-width:680px`で既に幅を制限しているため、そもそも不要だった）
- `.art-conclusion`・`.art-stat`・`.art-title`・`.art-body p`・`.art-meta`に`overflow-wrap:break-word`等を追加
- `html, body`に`overflow-x:hidden`を保険として追加し、今後similar な原因不明の幅はみ出しが起きてもページ全体が横スクロールしないようにした
- **教訓：** 日本語（CJK）コンテンツを含むページで`ch`単位を幅指定に使うのは避ける。`em`・`rem`・`%`・`min()`の方が安全。

### ホーム画面アプリが更新後も古いまま表示される問題（2026-08-15発生・対策済み）

デザイン修正後、本人がホーム画面のアプリアイコンから開いても新しいデザインが反映されない、という報告が複数回あった。原因は2つ重なっていた：
1. **bfcache（ページの凍結復元）**：iOSはホーム画面アプリをバックグラウンドから再開する際、ネットワークへ取りに行かず以前のページをそのまま復元することがある。→ `pageshow`イベントの`persisted`フラグで検知し、その場合は強制リロードする対策を実装済み。
2. **HTTPキャッシュ**：GitHub Pagesは`Cache-Control: max-age=600`（10分）を返す。この値の範囲内、あるいはiOSのホーム画面アプリ特有のキャッシュの粘りにより、強制終了→再起動しても古いHTTPキャッシュを再利用してしまうことがあった。→ **セッションにつき1回、URLにキャッシュ回避用のクエリパラメータ（`?_r=タイムスタンプ`）を付けて`location.replace`で再読み込みする**対策を追加。クエリ文字列が変わるとブラウザは別URLとして扱いHTTPキャッシュを迂回するため、確実に最新のHTML/CSS/JSを取得できる。`sessionStorage`で1セッション1回に制限し、無限リダイレクトを防いでいる。

この2つの対策により、今後は本人が手動でWebサイトデータを削除したりアイコンを作り直したりする必要はなくなるはずである（2026-08-15時点でコードは修正済み。次回以降の実際の効果は要観察）。

### カード右端のテキスト切れ（真の原因・2026-08-15修正）

`56ch`単位を撤去してもなお「文字が見切れる」報告が続いた。**本当の原因は`.articles li`にかけていた`overflow:hidden`だった。** 角丸カードの見た目を作るために付けていたが、この指定は「はみ出た分を折り返す」のではなく「はみ出た分を問答無用で切り取って隠す」ため、わずかな幅の計算ズレがあっても文字が折り返されず、途中でぷつっと消えるように見えていた（ユーザーが送ってくれたスクリーンショットで「オルガノイド技術の砳」のように文字が変な形に途切れていたのはこのため）。
修正：`.articles li`から`overflow:hidden`を撤去し、代わりに展開時の`.art-body`にだけ個別の`border-radius:0 0 11px 11px`を付けて角丸の見た目を維持した。あわせて`.art-meta .journal`にも`min-width:0`と`overflow-wrap:break-word`を追加。
**教訓：** `overflow:hidden`を角丸トリックのためだけに使うと、そこで本当に何かがはみ出ていたときにエラーとして気づけず、無言でテキストが消える形になる。デバッグ時は一時的に`overflow:visible`にして「本当は何がはみ出ているか」を目視してから対処するとよい。

**さらに追記（同日、根本的な構造修正）：** 上記の対処後もなお、本人から複数回「まだはみ出ている」という報告が続いた（Safari・Chrome双方、キャッシュ対策も試した上で）。原因の切り分けのため`curl`でサーバー側の配信内容を直接確認したところ修正は正しく反映されていたが、それでもなお実機で崩れる報告があった。
最終的に、`.art-meta`・`.art-conclusion`・`.art-stat`・`.art-title`にそれぞれ個別に`margin-left:1.5rem`と個別のmax-width調整を付けていた構造そのものが、要素ごとに計算のズレを生みうる脆い作りだったと判断し、**1つのラッパー`.art-summary`（`padding-left:1.5rem; box-sizing:border-box; width:100%;`）に統一**する構造修正を行った。子要素からは`margin-left`と`max-width`の個別指定を全て撤去し、上下の余白（`margin-top`）だけを残した。これにより幅の計算が一箇所に集約され、要素ごとのズレが起きなくなった。
**教訓：** 同じ字下げを複数の兄弟要素にバラバラに`margin-left`で付け、さらに個別に`max-width`を調整するのは、後から見て挙動を予測しづらく壊れやすい。共通の字下げは親のラッパーに`padding`として一箇所にまとめる方が堅牢。

**さらに続報（同日）：** 上記の`.art-summary`ラッパー化のあと、本人からiPhone 15のChromeで実際のスクリーンショットが送られてきて確認したところ、**実際にはテキストの見切れ（クリッピング）はもう起きていなかった。** 指摘の実体は「はみ出し」ではなく、**「＋」アイコン分の字下げ（`padding-left:1.5rem`）によって本文が左右非対称に右へ寄って見え、バランスが悪い**という見た目の指摘だった（複数回のやり取りで問題の性質そのものが変わっていたのに、当方が同じ「overflow」という前提で対処し続けていたのが手間取った原因）。
最終的に、「＋」アイコンを独立した行にする構造をやめ、**アイコンと本文をflexで横並びにする素直な構造**（`.art-toggle{display:flex}`、アイコンは`flex:none`、本文側`.art-summary`は`flex:1 1 auto; min-width:0;`）に変更した。これで左右の余白が自然に対称になり、見た目のバランスも改善し、副次効果としてflexアイテムの`min-width:0`により折り返しの確実性も上がった。
**教訓：** ユーザーが同じ言葉（「はみ出ている」）を使っていても、実際のスクリーンショットを見るまでは指摘の実体（クリッピングなのか、余白バランスの違和感なのか）を決めつけない方がよい。文章だけでの往復より、早い段階でスクリーンショットを求めた方が近道だった。

---

## 確認済みのアーキテクチャ（2026-08-15時点）

1. **収集**：
   - 最新論文：PubMed E-utilities（`esearch` → `efetch`）
   - クラシック論文：Europe PMC REST API（`CITED desc`ソート）
   - いずれも認証不要、PowerShellの`Invoke-RestMethod`やBashの`curl`で十分。動作確認済み。
2. **重複防止**：`docs/articles.json`を参照し、30日以内に使用済みのPMIDを除外。
3. **要約・翻訳**：`claude -p`（ヘッドレスClaude Code、既存のX自動化と同じ呼び出し方）でアブストラクトから「結論見出し」「補強数値」「タイトル和訳」「全文訳」を生成。
4. **保存・公開**：今日選定した論文を`docs/articles.json`に追記（既存エントリは変更しない）。`docs/index.html`は固定テンプレートで日々編集しない。git commit・pushしてGitHub Pagesで公開。
5. **通知**：当面なし（本人がページを直接確認する運用）。将来的にLINE通知を追加する場合は別途相談。
6. **自動実行**：Windowsタスクスケジューラで毎日、`.local/run-daily-digest.ps1`（Xの`run-daily-post.ps1`と同じパターン）が`claude.exe -p`をヘッドレス呼び出しし、収集〜選定〜要約・翻訳〜ファイル更新〜git pushまでを1セッションで完結させる。

---

## 自動化の実地検証（2026-08-15完了）

`.local/run-daily-digest.ps1` を実際に1回手動実行し、収集〜選定〜翻訳〜`docs/index.html`書き換え〜`資料/使用履歴.md`追記〜git commit・pushまでが**完全ノーミスで動くことを確認済み**。実行結果：
- 最新論文1本（リンチ症候群へのアスピリンCaPP3試験、Lancet Gastroenterol Hepatol）＋クラシック論文4本（CANONIC研究・大腸オルガノイド培養・GLOBOCAN大腸癌疫学・NAFLD疾病負荷予測モデル）を新規に選定
- コミット`d30ce5f`としてpush成功、GitHub Pages（https://taisuke1997t-code.github.io/gi-paper-digest/ ）にも数分後に反映されたことを確認
- 既存の未コミット変更（CLAUDE.mdの編集中の内容や`.gitignore`）には一切触れず、対象ファイルのみを正しくステージ・コミットしていた

これでタスクスケジューラ経由の完全無人実行も同じように動作するはずである（人間が画面の前にいなくても、`claude.exe -p --dangerously-skip-permissions`が同じ手順を辿るため）。

**その直後に判明した問題と修正（同日2026-08-15）：** この実地検証で、1回目の実行（9本）が2回目の実行（5本）によって完全に上書きされ、閲覧できなくなっていたことが本人からの指摘で発覚した。「過去の論文も見られるようにしてほしい、検索機能も欲しい」という要望を受け、上記「データ構造とアーカイブ」の章の通り、`docs/articles.json`への追記方式にアーキテクチャを変更した。`.local/run-daily-digest.ps1`のプロンプトもこの新方式に合わせて書き換え済み（次回の自動実行分から新方式が適用される）。1回目・2回目で生成した計14本のデータは`articles.json`に移行・保全済み。

## 未着手・次にやること

- [x] GitHubリポジトリ`gi-paper-digest`の作成・初回push・Pages有効化（2026-08-15完了。公開URL：https://taisuke1997t-code.github.io/gi-paper-digest/ 、リポジトリ：https://github.com/taisuke1997t-code/gi-paper-digest ）
- [x] `.local/run-daily-digest.ps1`の実装＋実地検証（2026-08-15完了。Xの`run-daily-post.ps1`をベースに、PubMed＋Europe PMCでの収集・30日重複除外ロジックを盛り込んだプロンプトを作成し、実際に1回手動実行して成功を確認。その後articles.json方式への変更に合わせてプロンプトも改訂済み（改訂後の内容自体はまだ実地検証していない——次回の自動実行で確認予定）。**注意：** `.ps1`ファイルはUTF-8 BOM付きで保存すること——BOMなしだとWindows PowerShell 5.1が日本語ヒアドキュメント内の文字を誤読し、`missing terminator`エラーで構文エラーになった実例あり）
- [x] アーカイブ・検索機能の実装（2026-08-15完了。`docs/articles.json`への追記方式に変更し、「今日のダイジェスト」＋「アーカイブ・検索」の2セクション構成にした。テキスト検索・タイプ絞り込み・ページネーション実装済み）
- [ ] articles.json追記方式での自動実行を1回実地検証する（前回の実地検証はHTML直接書き換え方式だったため、新方式でも同様に無人実行できるか要確認）
- [ ] IFティア一覧の妥当性検証（現状の数値は概算・要確認）と、定期更新の仕組み
- [ ] Tier S（NEJM/Lancet/JAMA）からクラシック論文を自動検索するクエリの確立（現状はGI専門誌のみ自動化できていて、総合誌からの発掘は手動）
- [ ] 1日あたりの配信本数の決定（未確定。デモでは5本）
- [x] Windowsタスクスケジューラへの登録（2026-08-15完了。タスク名：`医学論文アプリ_毎日ダイジェスト更新`、毎日21:30実行。Xの自動化（21:00）と30分ずらしてある。ウェイクタイマーは既存のXタスクと共通の電源設定に依存）
- [ ] （将来・保留）LINE通知の実装
- [ ] （将来）オープンアクセス論文の全文自動翻訳／購読誌の手動PDFアップロード運用
